import styled from 'styled-components';
import { animated } from "react-spring";

export const NavDropdownCont = styled(animated.div)`
  width: 100vw;
  position: relative;
  background-color: seashell;
  box-shadow: inset -3px -4px 0.25rem 0px rgba(61,66,80,.18);
  background-color: #f1f1f1;
  display: flex;
  justify-content: center;
  align-items: center;
`;